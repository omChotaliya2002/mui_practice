import { createTheme } from "@mui/material";

const theme = createTheme ({

    palette : {
        mode : "light",
        primary : {
            main : "#89CFF0",
        },
        secondary : {
            main : "#DA70D6",
        }
    }

});

export default theme;